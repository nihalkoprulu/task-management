import { FC } from "react";
import { HomepageContainer } from "./styled";

const Homepage: FC = () => {
  return (
    <HomepageContainer data-testid="homepage-container">Task</HomepageContainer>
  );
};

export default Homepage;
